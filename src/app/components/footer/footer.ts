import { Component } from '@angular/core';
import { SecNavbarComponent } from '../sec-navbar/sec-navbar';

@Component({
  selector: 'app-footer',
  imports: [SecNavbarComponent],
  templateUrl: './footer.html',
  styleUrl: './footer.scss',
})
export class Footer {

}
